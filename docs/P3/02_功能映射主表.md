# P3 功能映射主表（Functional Mapping Master Table）

> **目的：** 横向分工下，让"类图 / API / ER+SQL"三视角在**同一行**对齐。
> **使用方式：**
> 1. 组长**已填**：功能 ID、用户故事/SRS、模块、关键规则。
> 2. 组员**待填**（按交付物分工）：
>    - **A（类图负责人）** 填 _涉及类_ 列
>    - **B（API 负责人）** 填 _API 接口_ 列
>    - **C（ER+SQL 负责人）** 填 _涉及表_ 列
>    - **D（整合负责人）** 负责追踪每行三列是否都填齐，发现冲突拉会
> 3. 任何一行三列填完后，互审签字（在"互审"列写姓名）。
> 4. **整合阶段**直接基于本表逐行对账，不允许遗漏或新增。
> **图例：** ✓ = P0（Must） / ★ = P1（Should） / ◇ = P2（Could） / ✗ = Won't（本期不做，记录但不实现）

---

## 总览

- **功能总数：** 65（P0：38 / P1：21 / P2：4 / Won't：2）
- **覆盖模块：** 11 + 1 跨模块共用（auth / user / task / trade / edu / team / im / notify / credit / report / admin / search）
- **覆盖用户故事：** US-01 ~ US-20（共 20 条）
- **完成判据：** 每行三列必填；P0 行必须互审通过；P1/P2 行可标"延后"。

---

## 1. auth 模块（鉴权）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-AUTH-01 | ✓ | FR-AUTH-01 | 发送短信验证码；单手机号 60s ≤ 1 次 / 24h ≤ 20 次；单 IP 60s ≤ 5 次 / 24h ≤ 200 次 | `AuthController`, `AuthServiceImpl`, `SmsService`, `AuthSmsCodeRepository` | `auth_sms_code`（骨架）；防爆破计数：Redis（降级 DB） | `POST /api/auth/sms-codes` | D |
| F-AUTH-02 | ✓ | US-14, FR-AUTH-01 | 手机号 + 验证码登录/注册；首次登录自动建账，初始为"未认证游客"；返回 JWT + verifyStatus | `AuthController`, `AuthServiceImpl`, `JwtService`, `AuthUserRepository` | `auth_user`（phone_hmac, phone_cipher, password_hash, verify_status） | `POST /api/auth/token`、`POST /api/auth/register`、`POST /api/auth/token/password` | D |
| F-AUTH-03 | ✓ | US-14, FR-AUTH-02 | 提交学生证认证（≤ 3 张图 + 真实姓名 + 学号 + 证件类型）；状态 = pending；30 日内自动删图 | `AuthController`, `AuthServiceImpl`, `AuthVerificationRepository`, `ObjectStorageClient` | `auth_verification`（密文+attachment_sha256） | （openapi 待迭代补：`POST /api/auth/verifications`） | D |
| F-AUTH-04 | ✓ | US-14, FR-AUTH-02 | 查询当前认证状态（pending/approved/rejected + 驳回理由） | `AuthController`, `AuthServiceImpl`, `AuthVerificationRepository` | `auth_verification` | （待补：`GET /api/auth/verifications/me`） | D |
| F-AUTH-05 | ✓ | FR-AUTH-01 | JWT 续签（refresh token） | `AuthController`, `AuthServiceImpl`, `JwtService` | （无表，依赖 Redis token 黑名单可选） | （待补：`POST /api/auth/token/refresh`） | D |
| F-AUTH-06 | ✓ | — | 退出登录（前端清 token；后端可选黑名单短期） | `AuthController`, `TokenBlacklistService`（可选） | （无表；可选黑名单 Redis） | （待补：`POST /api/auth/logout`） | D |

---

## 2. user 模块（用户资料 / 隐私）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-USER-01 | ✓ | US-13, FR-AUTH-03 | 修改昵称 / 头像；昵称 2~20 字，禁敏感词；头像 ≤ 2MB | `UserController`, `UserServiceImpl`, `UserProfileRepository` | `user_profile`（nickname, avatar_url） | （待补：`PATCH /api/users/me/profile`） | D |
| F-USER-02 | ✓ | US-13.4 | 三项隐私开关（隐藏发布历史/接单记录/课程评价）**默认打开**；变更立即生效 + 写审计日志 | `UserController`, `UserServiceImpl`, `UserProfileRepository`, `UserAuditLogRepository` | `user_profile`（hide_publish_hist/hide_accept_hist/hide_course_reviews）、`user_audit_log` | （待补：`PATCH /api/users/me/privacy`） | D |
| F-USER-03 | ✓ | US-13.2 | 查看他人公开主页：仅 `PublicUserVO`（昵称 + 头像 + 已认证标签），禁出真实姓名/学号 | `UserController`, `UserServiceImpl`, `PublicUserVO` | `user_profile`（仅公开字段）、`auth_user.verify_status` | （待补：`GET /api/users/{userId}/public`） | D |
| F-USER-04 | ✓ | — | 查看我的主页（含完整非脱敏字段，仅本人可见） | `UserController`, `UserServiceImpl`, `UserProfileRepository` | `user_profile`（全字段，本人） | （待补：`GET /api/users/me`） | D |

---

## 3. task 模块（跑腿 / 万能互助 / 状态机）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-TASK-01 | ✓ | US-01, FR-TASK-01 | 发布跑腿任务；5 必填字段；调用 `creditApi.getScoreOf < 60 → 403`；调用 `creditApi.freeze` 冻结悬赏积分 | `TaskController`, `TaskServiceImpl`, `TaskRepository`, `TaskStateContext`, `CreditApi` | `task_order`（写）、`credit_account`+`credit_record`（freeze） | `POST /api/tasks` | D |
| F-TASK-02 | ✓ | US-01.5 | 待接单状态可编辑（送达楼栋 / 截止时间 / 备注），状态进入"进行中"后禁改 | `TaskServiceImpl`, `TaskStateContext`, `PendingAcceptState`, `TaskRepository` | `task_order`（局部更新） | （待补：`PATCH /api/tasks/{taskId}`） | D |
| F-TASK-03 | ✓ | US-02, FR-TASK-03 | 抢单（乐观锁 `version`）；并发零冲突；自接禁止；进行中任务 > 上限 → 拒绝；接单后调用 `creditApi.freeze` 冻接单方押金 | `TaskServiceImpl`, `PendingAcceptState`, `TaskRepository`, `CreditApi`, `UserApi` | `task_order`（更新 + version）、`credit_account`+`credit_record`（freeze）、`user_profile.daily_accept_limit` | `POST /api/tasks/{taskId}/accept` | D |
| F-TASK-04 | ✓ | US-19 | 上传完成凭证（≤ 3 图 + ≤ 300 字说明） → 状态 `WAIT_CONFIRM` | `TaskServiceImpl`, `InProgressState`, `WaitConfirmState`, `TaskRepository`, `TaskAttachment` | `task_order`、`task_attachment` | （待补：`POST /api/tasks/{taskId}/proof`） | D |
| F-TASK-05 | ✓ | US-19 | 发布者确认完成 → 触发 `TaskCompletedEvent` → `creditApi.settle` 结算 | `TaskServiceImpl`, `WaitConfirmState`, `TaskCompletedEvent`, `CreditTaskListener`, `CreditApi` | `task_order`、`credit_account`+`credit_record`（settle） | （待补：`POST /api/tasks/{taskId}/confirm`） | D |
| F-TASK-06 | ✓ | US-19 | 取消任务（仅未接单 / 双方协商）→ 触发 `TaskCanceledEvent` → 解冻积分 | `TaskServiceImpl`, `TaskStateContext`, `TaskCanceledEvent`, `CreditTaskListener` | `task_order`、`credit_account`+`credit_record`（unfreeze） | （待补：`POST /api/tasks/{taskId}/cancel`） | D |
| F-TASK-07 | ✓ | US-19 | 超时自动流转 → `EXPIRED`；判定精度 ≤ 5 分钟；冻结积分维持原状；进入仲裁队列 | `TaskTimeoutScanner`, `TaskServiceImpl`, `ExpiredState`, `TaskExpiredEvent` | `task_order`（status→EXPIRED）、`report_case`（进入仲裁队列） | （无前台 API，定时扫描内部行为） | D |
| F-TASK-08 | ✓ | US-19.5 | 发布者主动延长截止时间（≤ 2 次，每次 ≤ 2h） | `TaskServiceImpl`, `TaskExtendLog`, `TaskStateContext`, `InProgressState` | `task_order.deadline_at`、`task_extend_log` | （待补：`POST /api/tasks/{taskId}/extend`） | D |
| F-TASK-09 | ✓ | US-02.5 | 个人接单上限可配置（1~3）；调整对新接单生效 | `TaskServiceImpl`, `UserProfileRepository`, `UserApi` | `user_profile.daily_accept_limit` | （待补：`PATCH /api/users/me/accept-limit`） | D |
| F-TASK-10 | ★ | US-11 | 万能互助发单（"其他"分类）；自定义悬赏 ≤ 500 积分；24h 发布次数 > 10 触发风控 | `TaskServiceImpl`, `RiskControlService`（P1 补图） | `task_order`（task_type=互助） | （P1 复用 `POST /api/tasks`，参数 `taskType=MUTUAL_HELP`） | D |
| F-TASK-11 | ✓ | — | 浏览任务大厅（含分页、筛选交给 search 模块） | `TaskController`, `SearchServiceImpl`, `TaskApi` | `task_order`（读，走 search） | `GET /api/search/tasks` | D |
| F-TASK-12 | ✓ | — | 查看任务详情（含发布者 PublicUserVO、状态、悬赏、附件） | `TaskController`, `TaskServiceImpl`, `UserApi`, `PublicUserVO` | `task_order`、`task_attachment`、`user_profile`（PublicUserVO） | `GET /api/tasks/{taskId}` | D |

---

## 4. trade 模块（二手交易）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-TRADE-01 | ✓ | US-03, FR-AUTH-03 | 发布二手商品；≤ 9 图、单图 ≤ 5MB；EXIF 自动清洗；取货地点（精确宿舍/楼栋范围/面交）默认楼栋范围 | `TradeController`, `TradeServiceImpl`, `TradeItemRepository`, `ExifCleaner` | `trade_item` | （待补：`POST /api/trade/items`） | D |
| F-TRADE-02 | ✓ | US-03 | 商品上下架管理（仅本人） | `TradeController`, `TradeServiceImpl`, `TradeItemRepository` | `trade_item.status` | （待补：`PATCH /api/trade/items/{itemId}/status`） | D |
| F-TRADE-03 | ✓ | US-04, FR-MSG-01 | 议价后下单：进入会话先议价 → 一键下单 → `creditApi.freeze` 冻结买家积分 → 状态 `IN_TRADE` | `TradeController`, `TradeServiceImpl`, `TradeOrderRepository`, `CreditApi`, `ImApi` | `trade_order`、`credit_account`+`credit_record`（freeze）、`im_conversation` | `GET /api/trade/orders/{orderId}`（详情）；（待补：`POST /api/trade/orders`） | D |
| F-TRADE-04 | ✓ | US-04 | 面交完成确认（双方任一确认 → 进入待对方确认；双方确认 → 结算） | `TradeServiceImpl`, `TradeOrderRepository`, `CreditApi` | `trade_order.status`、`credit_account`+`credit_record`（settle） | （待补：`POST /api/trade/orders/{orderId}/confirm`） | D |
| F-TRADE-05 | ★ | US-04.5 | 卖家不得因议价被拒给买家差评/扣信用（系统级规则约束） | `TradeServiceImpl`, `ReviewRuleValidator`（P1） |   |   |   |
| F-TRADE-06 | ★ | US-04.6 | IM 共享地点卡片（下拉选公共地标） — 实现见 `im` 模块 | `ImServiceImpl`, `LocationCardDTO`（P1） |   |   |   |

---

## 5. edu 模块（学习社区：评价 / 资料 / 辅导）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-EDU-01 | ★ | US-08, FR-EDU-01 | 上传学习资料；白名单后缀（PDF/PPT/DOC/ZIP）；单文件 ≤ 50MB；按"院系-专业-课程名"归档 | `EduResourceService`, `EduResourceRepository`（P1） |   |   |   |
| F-EDU-02 | ★ | US-08 | 资料下载 / 感谢计数 | `EduResourceService`（P1） |   |   |   |
| F-EDU-03 | ★ | US-07, FR-EDU-02 | 提交课程评价（三维度：给分/作业量/趣味性；1~5 星）；匿名前台不可见作者 | `EduReviewService`, `EduReviewRepository`（P1） |   |   |   |
| F-EDU-04 | ★ | US-07 | 浏览课程评价（按时间/最有用排序，分页 20/页） | `EduReviewService`（P1） |   |   |   |
| F-EDU-05 | ✓ | US-05, FR-EDU-03 | 发布辅导需求；违禁词同步拦截（含谐音/全角/emoji 兜底）；3 次触发冷静 24h | `EduController`, `TutorTaskServiceImpl`, `ForbiddenWordMatcher`, `EduTutorTaskRepository`, `TaskService` | `edu_tutor_task`、`task_order`（底层复用）、`admin_forbidden_word`（命中） | （待补：`POST /api/edu/tutor-tasks`） | D |
| F-EDU-06 | ★ | US-06 | 辅导任务列表自动过滤违禁词命中项 + 信用分 < 60 发布者；可勾"包含低信用分" | `SearchServiceImpl`, `TaskApi`, `CreditApi`（P1） |   |   |   |
| F-EDU-07 | ★ | US-06 | 辅导任务接单（复用 task 状态机） | `TaskServiceImpl`, `TaskStateContext`（P1） |   |   |   |

---

## 6. team 模块（比赛组队）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-TEAM-01 | ★ | US-09, FR-TASK-01 | 发布招募；技能标签 1~5 个；指定队伍总人数；发布后向同标签用户弱推送 | `TeamRecruitController`, `TeamRecruitServiceImpl`（P1 类图延后） |   |   |   |
| F-TEAM-02 | ★ | US-10, FR-MSG-02 | 申请加入；附带申请者标签 + 信用分；同帖 24h ≤ 3 次 | `TeamApplicationServiceImpl`（P1） |   |   |   |
| F-TEAM-03 | ★ | US-10 | 队长审核（同意/拒绝） → 推送通知 | `TeamApplicationServiceImpl`, `NotifyServiceImpl`（P1） |   |   |   |
| F-TEAM-04 | ★ | US-09 | 成员数 ≥ 总人数 → 自动"已满员"，搜索不可见 | `TeamRecruitServiceImpl`, `SearchServiceImpl`（P1） |   |   |   |

---

## 7. im 模块（站内私信）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-IM-01 | ✓ | US-02, FR-MSG-01 | 接单后 2 秒内自动创建任务关联会话；自动推送"已接单"系统消息 | `TaskAcceptedImListener`, `ImServiceImpl`, `ImConversationRepository` | `im_conversation`、`im_message`（系统消息） | （内部事件 `TaskAcceptedEvent` 触发；无对外 API） | D |
| F-IM-02 | ✓ | US-04 | 发送文本/图片/表情；图片 ≤ 5MB | `ImController`, `ImServiceImpl`, `ImMessageRepository` | `im_message`、`im_conversation.last_msg_at` | （待补：`POST /api/im/conversations/{id}/messages`） | D |
| F-IM-03 | ★ | US-04.6 | 共享地点卡片（公共地标下拉，禁手输具体门牌） | `ImServiceImpl`, `LocationCardDTO`（P1） | `im_message`（content_type=LOCATION） | （P1，复用消息接口） | D |
| F-IM-04 | ✓ | — | 未读消息计数 | `ImServiceImpl`, `ImConversationRepository`, `ImUnreadCounter` | `im_unread_count` | （待补：`GET /api/im/unread`） | D |
| F-IM-05 | ✓ | US-04.4 | 私信权限关闭检测：发送时给明确提示并阻止 | `ImServiceImpl`, `UserProfileRepository` | `user_profile`（隐私开关查） | （在发送接口前置校验，无独立 API） | D |
| F-IM-06 | ✓ | US-15.5 | 一键引用聊天记录作为申诉证据（`report` 调用） | `ImServiceImpl`, `ImApi`, `ReportServiceImpl` | `im_message`（读）、`credit_appeal.evidence_json` | （内部 `ImApi.exportEvidenceSnippet`；前台经 `POST /api/credit/appeals`） | D |
| F-IM-07 | ✓ | US-04 | 聊天记录持久化 ≥ 180 天；管理员后台可调取 | `ImServiceImpl`, `ImMessageRepository`, `ImApi` | `im_message`（保留 ≥180 天） | （管理端：`GET /api/admin/im/messages?caseId=...`） | D |

---

## 8. notify 模块（系统通知 / 站内信）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-NOTIFY-01 | ✓ | — | 监听各业务事件（`AFTER_COMMIT + @Async`），写入站内信 | `NotifyTaskListener`, `NotifyReportListener`, `NotifyServiceImpl` | `notify_message`（写） | （内部 Listener，无前台 API） | D |
| F-NOTIFY-02 | ✓ | — | 系统通知列表（分页） | `NotifyController`, `NotifyServiceImpl`, `NotifyMessageRepository` | `notify_message`（读） | （待补：`GET /api/notify/messages`） | D |
| F-NOTIFY-03 | ✓ | — | 标记单条 / 全部已读 | `NotifyController`, `NotifyServiceImpl` | `notify_message.read_at` | （待补：`PATCH /api/notify/messages/{id}/read`、`POST /api/notify/messages/read-all`） | D |
| F-NOTIFY-04 | ✓ | US-19.4 | 任务超时前 10 分钟"温柔提醒"接单者 | `TaskReminderScheduler`, `NotifyServiceImpl`, `TaskRepository` | `task_order.deadline_at`、`notify_message` | （内部定时任务，无前台 API） | D |
| F-NOTIFY-05 | ★ | US-09 | 招募发布后向同标签用户**弱推送**（不产生强通知） | `TeamRecruitServiceImpl`, `NotifyServiceImpl`（P1） | `notify_message`、`team_recruit.tags_json` | （P1，复用通知接口） | D |

---

## 9. credit 模块（信用分 / 积分账户 / 申诉）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-CREDIT-01 | ✓ | US-16, FR-CRED-01 | 查询信用分（含 dailyAcceptLimit / canPublish / canAccept；< 80 限单 1 / < 60 禁发禁接） | `CreditController`, `CreditServiceImpl`, `CreditAccountRepository` | `credit_account` | （待补：`GET /api/credits/me`） | D |
| F-CREDIT-02 | ◇ | US-16 | 信用分历史曲线（≥ 180 天；按周/月切换；每点显示原因） | `CreditHistoryService`, `CreditScoreLogRepository`（P2） | `credit_score_log` | （P2，待补：`GET /api/credits/me/score-history`） | D |
| F-CREDIT-03 | ✓ | — | **内部 Api：** 积分冻结 / 解冻 / 结算（`CreditApi.freeze/unfreeze/settle`） | `CreditServiceImpl`, `CreditApi`, `CreditTaskListener`, `CreditRecordRepository` | `credit_account`、`credit_record`（uk_credit_record_biz 幂等） | （内部 `CreditApi`，无前台 API；BEFORE_COMMIT） | D |
| F-CREDIT-04 | ✓ | — | **内部 Api：** 信用分扣减（`CreditApi.deduct(reasonCode)`，Strategy 模式） | `CreditServiceImpl`, `DeductStrategy`, `DeductStrategyRegistry`, `CreditScoreLogRepository` | `credit_account.credit_score`、`credit_score_log` | （内部 `CreditApi.deduct(...)`，无前台 API） | D |
| F-CREDIT-05 | ✓ | US-15 | 发起差评申诉；7 日内 / 同事件 ≤ 3 次；超期或超次返回 422 | `CreditController`, `CreditServiceImpl`, `CreditAppealRepository` | `credit_appeal` | （待补：`POST /api/credit/appeals`） | D |
| F-CREDIT-06 | ✓ | US-15.1 | 申诉证据上传（≤ 5 图 + ≤ 500 字） | `CreditServiceImpl`, `CreditAppealRepository`, `ObjectStorageClient` | `credit_appeal.evidence_json` | （待补：`POST /api/credit/appeals/{id}/evidence`） | D |
| F-CREDIT-07 | ✓ | US-15.6 | 申诉中差评在公开页面打码"该评价正在申诉中" | `CreditServiceImpl`, `ReviewPublicAssembler` | `task_review`、`credit_appeal.status` | （详情接口装配层处理，复用 `GET /api/tasks/{taskId}`） | D |
| F-CREDIT-08 | ✓ | — | 积分流水查询（冻结/解冻/结算/扣减记录） | `CreditController`, `CreditRecordRepository` | `credit_record` | （待补：`GET /api/credits/me/records`） | D |

---

## 10. report 模块（举报 / 仲裁）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-REPORT-01 | ✓ | — | 提交举报（违规内容/用户/任务） → 触发 `ReportSubmittedEvent` | `ReportController`, `ReportServiceImpl`, `ReportCaseRepository`, `ReportSubmittedEvent` | `report_case` | （待补：`POST /api/reports`） | D |
| F-REPORT-02 | ✓ | US-18, FR-ADM-03 | 仲裁工作台（管理员）：查看订单全流程 + 双方私信 + 证据 | `AdminArbitrationController`, `ReportServiceImpl`, `TaskApi`, `TradeApi`, `ImApi` | `report_case`、`task_order` / `trade_order` / `im_message`（聚合） | （待补：`GET /api/admin/arbitration/cases/{caseId}`） | D |
| F-REPORT-03 | ✓ | US-18 | 仲裁裁决执行：强制结算 / 强制退款 / 维持原状；10 秒内积分流转完成（`BEFORE_COMMIT`） | `ReportServiceImpl`, `CreditApi`, `ReportDecisionRepository` | `report_decision`、`credit_account`+`credit_record` | （待补：`POST /api/admin/arbitration/cases/{caseId}/decision`） | D |
| F-REPORT-04 | ✓ | US-18 | 仲裁记录永久存档；不可编辑、不可删除 | `ReportDecisionRepository`, `ReportDecision` | `report_decision`（DDL 禁更新/删除约束 + 审计） | （只读：复用仲裁工作台接口） | D |
| F-REPORT-05 | ✓ | US-15 | 申诉结果通知（含理由 + 可申诉次数） | `CreditServiceImpl`, `NotifyServiceImpl`, `CreditAppealRepository` | `credit_appeal.status`、`notify_message` | （内部事件触发；前台用 `GET /api/notify/messages`） | D |

---

## 11. admin 模块（管理后台）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-ADMIN-01 | ✓ | US-17, FR-ADM-01 | 审核学生证认证（通过/驳回 + 理由 ≥ 5 字）；2 秒内推送通知；标签授予"校园已认证" | `AdminVerificationController`, `AuthServiceImpl`, `UserVerifiedEvent`, `NotifyServiceImpl` | `auth_verification.status`、`auth_user.verify_status`、`notify_message` | （待补：`POST /api/admin/auth/verifications/{ticketId}/approve`、`/reject`） | D |
| F-ADMIN-02 | ✓ | — | 用户封禁 / 解封 | `AdminUserController`, `AuthUserRepository`, `UserProfileRepository` | `auth_user.banned`、`admin_operation_log` | （待补：`POST /api/admin/users/{userId}/ban`、`/unban`） | D |
| F-ADMIN-03 | ✓ | US-05.4 | 内容审核（违禁词命中队列）：人工二次判定 | `AdminContentController`, `ForbiddenWordHitRepository`, `TutorTaskServiceImpl` | `admin_forbidden_word`、`task_order.status` | （待补：`GET /api/admin/content/forbidden-hits`、`POST .../resolve`） | D |
| F-ADMIN-04 | ✓ | US-17.4 | 操作审计日志（操作人 / 时间 / IP / 理由）；不可删 | `AdminAuditAspect`, `AdminOperationLogRepository` | `admin_operation_log` | （AOP 切面自动记录；查询：`GET /api/admin/audit-logs`） | D |
| F-ADMIN-05 | ★ | — | 配置中心（违禁词列表、风控阈值、自定义悬赏上限） | `AdminConfigController`, `AdminConfigRepository`（P1） | `admin_config`、`admin_forbidden_word` | （P1） | D |
| F-ADMIN-06 | ✓ | US-12.5 | 树洞匿名违规：删帖 + 扣信用分（不破坏匿名性） | `AdminWallController`, `WallPostRepository`, `CreditApi`（模块归属待定） | `wall_post`、`credit_score_log` | （待补，wall 模块归属待定） | D |
| F-ADMIN-07 | ✓ | US-18 | 超时任务进入仲裁队列；48h 内必须人工处理；超时置红 | `ReportServiceImpl`, `ReportCaseRepository`, `TaskTimeoutScanner` | `report_case`（status=URGENT 标红） | （仲裁工作台接口；定时扫描内部） | D |

---

## 12. search 模块（搜索 / 筛选）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-SEARCH-01 | ✓ | US-06 | 任务大厅多维筛选（类型 / 信用分阈值 / 状态 / 时间范围） | `SearchController`, `SearchServiceImpl`, `TaskApi` | `task_order`、`credit_account.credit_score`（JOIN/服务层装配） | `GET /api/search/tasks` | D |
| F-SEARCH-02 | ★ | US-08.3 | 课程关键词检索（课程名 + 年份） | `SearchServiceImpl`, `EduResourceRepository`（P1） |   |   |   |
| F-SEARCH-03 | ★ | — | 二手商品检索（关键词 + 分类 + 楼栋范围） | `SearchServiceImpl`, `TradeApi`（P1） |   |   |   |
| F-SEARCH-04 | ★ | US-09 | 组队招募标签筛选 | `SearchServiceImpl`, `TeamApi`（P1） |   |   |   |

---

## 13. 树洞 / 匿名（归属待定，建议挂在 `news` 或新增 `wall` 模块；P3 阶段视情况降级）

| 功能 ID | 优先级 | 用户故事 / SRS | 关键规则 / 验收要点 | 涉及类（A 填） | 涉及表（C 填） | API 接口（B 填） | 互审 |
| :--- | :---: | :--- | :--- | :--- | :--- | :--- | :--- |
| F-WALL-01 | ★ | US-12, FR-NEWS-01 | 匿名发帖：前台统一头像 + "匿名用户"昵称；管理员后台可回查 | `WallPostController`, `WallPostServiceImpl`（P1，`wall` 模块待定） |   |   |   |
| F-WALL-02 | ★ | US-12.2 | 敏感词命中 → 二级人工审核 | `WallPostServiceImpl`, `AdminContentController`（P1） |   |   |   |
| F-WALL-03 | ★ | US-12.3 | 单用户 24h ≤ 5 条匿名帖 | `WallPostServiceImpl`, `WallRateLimiter`（P1） |   |   |   |
| F-WALL-04 | ★ | US-12.4 | 截图水印（"CampusHub + 匿名 ID 哈希"） — 前端实现 | `WallScreenshotWatermark`（前端，P1） |   |   |   |

> **组长注：** P2 模块清单未单列"树洞"模块，团队需在第一次设计会上决定：
> 选项 A — 新增 `wall` 模块；选项 B — 合并入 `edu` 或独立 `news`；选项 C — 本期降级至 `[Should]` 暂不实现。
> **建议：** 选 A，独立 `wall` 模块，原因是匿名机制的隔离性高，与其他业务耦合代价低。

---

## 14. 不实现项（记录但不画图、不建表、不写 API）

| 功能 ID | 优先级 | 用户故事 / SRS | 说明 |
| :--- | :---: | :--- | :--- |
| F-RANK-01 | ✗ | US-20, FR-NEWS-02 | 热门推荐榜单 — 本期 Won't；下迭代候选 |

---

## 互审签字记录

| 轮次 | 日期 | 互审范围 | 冲突清单文件 | 拍板人 |
| :--- | :--- | :--- | :--- | :--- |
| 第 1 轮 | _____ | ER ↔ 类图 ↔ API（粗对账） | `冲突清单_R1.md` | 组长 |
| 第 2 轮 | _____ | 全表三列完整对账 | `冲突清单_R2.md` | 组长 |

---

## 完成度统计模板（每周更新一次）

| 模块 | 总行数 | 三列填齐 | 互审通过 | 完成率 |
| :--- | ---: | ---: | ---: | ---: |
| auth | 6 | 0 | 0 | 0% |
| user | 4 | 0 | 0 | 0% |
| task | 12 | 0 | 0 | 0% |
| trade | 6 | 0 | 0 | 0% |
| edu | 7 | 0 | 0 | 0% |
| team | 4 | 0 | 0 | 0% |
| im | 7 | 0 | 0 | 0% |
| notify | 5 | 0 | 0 | 0% |
| credit | 8 | 0 | 0 | 0% |
| report | 5 | 0 | 0 | 0% |
| admin | 7 | 0 | 0 | 0% |
| search | 4 | 0 | 0 | 0% |
| wall（待定） | 4 | 0 | 0 | 0% |
| **总计** | **79** | **0** | **0** | **0%** |

---

**本表最后修订：** 2026-05-14（整合阶段 D 回填 B/C 两列）
**说明：** 任何新增/删除功能行必须经过组长同意并在末尾追加变更记录。

**变更记录：**
- 2026-05-12 — 根据 [04_核心类图](./04_核心类图.md) 回填「涉及类」列（含 P0 全量与 P1/P2 占位）；
- 2026-05-14 — 整合阶段（组员 D）按 [schema.sql](./数据库/schema.sql) 与 [openapi.yaml](./API/openapi.yaml) 回填 P0/P1 行的「涉及表」「API 接口」两列。
  - 已交付 OpenAPI 的 9 个接口标注实际路径；未在 yaml 中的接口标注「（待补：<建议路径>）」，作为下一轮 B 同学迭代输入；
  - 仅在 ER 图/包结构骨架预留但 schema.sql 未建表的（如 `auth_sms_code`、`user_audit_log`、`im_*`、`notify_*`、`report_*`、`admin_*`、`edu_*`、`team_*`、`wall_*`、`credit_appeal`、`credit_score_log`），标注表名供 C 同学下一轮补 DDL；
  - 互审列统一标 `D`，表示整合阶段已完成三列交叉对账；第二轮全员互审签字见 [07_详细设计文档（整合版）](./07_详细设计文档（整合版）.md) §冲突清单。
